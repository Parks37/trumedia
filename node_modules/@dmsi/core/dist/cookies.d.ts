interface CookieOptions {
    expires?: Date;
    path?: string;
    onCookieSet?: (value: string) => void;
}
/**
 * Utility wrapper for document.cookie. These functions allow for easier access and modification
 * of cookies.
 */
declare const DMSICookies: {
    get: (key: string) => string | null;
    set: (key: string, val: string, options?: CookieOptions | undefined) => string | null;
    /**
     * Update cookie's expire time to the unix epoch + 1 (which we are sure has already passed).
     * By doing this, the cookie will expire and be removed from browser's cookie storage.
     */
    delete: (key: string) => string | null;
};
export default DMSICookies;
