import { OptionProps } from '../../../types';
declare type NewValueType = {
    multiple?: boolean;
    options: OptionProps[];
    filteredOptions?: OptionProps[];
    currentIdx: number;
    previousIdx: number;
    shiftDown: boolean;
    value: string[];
};
declare const getNewValue: ({ multiple, options, filteredOptions, currentIdx, previousIdx, shiftDown, value, }: NewValueType) => string[];
export default getNewValue;
