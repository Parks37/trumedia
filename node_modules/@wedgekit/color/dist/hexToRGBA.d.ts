/**
 * Converts a hex color code string to the matching RGBA string.
 *
 * @param color A color string to be converted
 * @param alpha An optional alpha value (in the range of 0 to 1 inclusive) to use instead of an alpha value in the hex string
 */
export default function hexToRGBA(color: string, alpha?: number | string): string | undefined;
