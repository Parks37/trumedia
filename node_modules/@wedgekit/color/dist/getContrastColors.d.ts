import { ColorTag } from './colorPalette';
import { ContrastColorTag } from './contrastColors';
declare type ContrastColors = {
    text: ColorTag;
    border?: ContrastColorTag;
};
/**
 * Get the appropriate text (and possibly border) color for a given color tag.
 *
 * @param tagColor The color tag you are working with
 * @param outline If an outline is present on the text
 */
export default function getContrastColors(tagColor: ColorTag, outline?: boolean): ContrastColors;
export {};
