import { ColorTag } from './colorPalette';
export declare type Context = {
    bg: ColorTag;
    text: ColorTag;
    hover: ColorTag;
    active: ColorTag;
    border: ColorTag;
    focus: ColorTag;
    focusInnerBorder: ColorTag;
    focusOuterBorder: ColorTag;
    disabled: ColorTag;
};
export declare type ContextDomain = 'primary' | 'secondary' | 'success' | 'warning' | 'danger' | 'default' | 'white';
/**
 * Get common groupings of colors based on a `ContextDomain`.
 *
 * @param domain the `ContextDomain` in which you are display
 */
export default function contextualize(domain: ContextDomain): Context;
