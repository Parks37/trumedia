import { NumberFormatValues } from 'react-number-format';
declare const isAllowed: (props: {
    decimals?: number;
    max?: number;
    min?: number;
}) => (valueProps: NumberFormatValues) => boolean;
export default isAllowed;
