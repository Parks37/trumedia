declare type ViewportEdge = 'top' | 'bottom' | 'left' | 'right';
declare type ClippedEdges = Partial<Record<ViewportEdge, number>>;
declare const getClippedEdges: (rect: ClientRect) => ClippedEdges;
export default getClippedEdges;
