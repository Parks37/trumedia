import React from 'react';
import type { MountedIndicator } from '../App/App';
declare type Props = {
    children: (state?: MountedIndicator) => React.ReactNode;
    resource: React.ReactNode;
    resourceMounted: boolean;
};
declare type PageProps = Props & {
    type: 'drawer' | 'drawer-mobile';
};
declare const Page: (props: PageProps) => React.ReactNode;
export default Page;
