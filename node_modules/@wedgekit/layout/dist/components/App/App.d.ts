import * as React from 'react';
export declare type MountedIndicator = {
    resourceMounted: boolean;
};
declare type Props = {
    children: (state: MountedIndicator) => React.ReactNode;
    header?: React.ReactNode;
    notifications?: React.ReactNode;
    resource: React.ReactNode;
    resourceMounted: boolean;
    resourceType?: 'drawer' | 'drawer-mobile';
    drawerLeft?: boolean;
    drawerWidth?: string;
};
declare function Layout(props: Props): JSX.Element;
declare namespace Layout {
    var defaultProps: {
        header: null;
        notifications: null;
        resourceType: string;
        drawerLeft: boolean;
    };
}
export default Layout;
