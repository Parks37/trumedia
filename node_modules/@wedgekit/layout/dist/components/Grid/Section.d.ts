import React from 'react';
import type { CSSProperties } from 'styled-components';
declare type SectionProps = {
    children?: React.ReactNode;
    /**
     * The grid-template area to which this section belongs
     */
    area: CSSProperties['gridArea'];
    align?: CSSProperties['alignSelf'];
    justify?: CSSProperties['justifySelf'];
    place?: CSSProperties['placeSelf'];
};
/**
 * Section provides a wrapper around a grid-area to be used
 * with LayoutNext.
 */
declare const Section: ({ children, ...otherProps }: SectionProps) => JSX.Element;
export default Section;
