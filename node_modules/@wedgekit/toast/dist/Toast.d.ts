import React from 'react';
import type { ColorPalette } from '@wedgekit/color';
import type { IconTag } from '@wedgekit/icons';
declare type Props = {
    className?: string;
    crust?: boolean;
    domainColors?: Partial<{
        bg: keyof ColorPalette;
        crust: keyof ColorPalette;
        icon: keyof ColorPalette;
    }>;
    extraHeaderContent?: React.ReactNode;
    header: string;
    icon?: IconTag;
    message?: string;
    remove: () => void;
};
declare const Toast: ({ className, header, crust, icon, message, remove, domainColors, extraHeaderContent, }: Props) => JSX.Element;
export default Toast;
