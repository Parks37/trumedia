import styled from 'styled-components';
import Layout from '@wedgekit/layout';
import color from '@wedgekit/color';

import type { ColorPalette } from '@wedgekit/color';

const Bar = styled(Layout.Section)<{ crust?: keyof ColorPalette }>`
  align-self: stretch;
  background-color: ${({ crust }: { crust?: keyof ColorPalette }) => color[crust ?? 'B700']};
  border-radius: 100px;
  width: 5px;
`;

export default Bar;
