import styled from 'styled-components';
import { getBasePx, Text } from '@wedgekit/primitives';

const StyledText = styled(Text)`
  line-height: 1.5em;
  margin: ${getBasePx(2)} ${getBasePx(6)};
`;

export default StyledText;
