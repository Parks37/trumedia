import styled from 'styled-components';

import color from '@wedgekit/color';
import { InteractiveWrapper } from '@wedgekit/icons';

const CloseWrapper = styled(InteractiveWrapper)`
  align-content: center;
  box-sizing: border-box;
  display: grid;
  height: 42px;
  justify-content: center;
  margin-right: 18px;
  width: 42px;

  &:hover,
  &:focus {
    background-color: ${color.N100};
    svg {
      fill: ${color.N600};
    }
  }
`;

export default CloseWrapper;
