import styled from 'styled-components';

import color, { hexToRGBA } from '@wedgekit/color';
import Layout from '@wedgekit/layout';
import { getBasePx } from '@wedgekit/primitives';

import type { ColorPalette } from '@wedgekit/color';

const IconWrapper = styled(Layout.Section)<{ bg: keyof ColorPalette }>`
  align-content: center;
  height: ${getBasePx(8)};
  width: ${getBasePx(8)};
  background-color: ${({ bg }) => (bg.startsWith('R') ? hexToRGBA(color[bg], 0.8) : color[bg])};
  border-radius: 50%;
  display: grid;
  margin-left: ${(crust) => (crust ? 18 : 16)}px;
  justify-content: center;
`;

export default IconWrapper;
