#!/usr/bin/env node
export declare type Package = {
    name: string;
    version: string;
    dependencies?: Record<string, string>;
    peerDependencies?: Record<string, string>;
};
export declare type GlobalOptions = {
    package?: boolean;
    script?: boolean;
};
export declare type CreateSubcommands = {
    name: string;
};
export declare type CreateOptions = GlobalOptions & {
    desc: string;
    dir?: string;
    template?: string;
};
export declare type BuildOptions = GlobalOptions & {
    clean?: boolean;
    dev?: boolean;
    ['types-only']?: boolean;
    unbundled?: boolean;
    watch?: boolean;
};
export declare type StartOptions = GlobalOptions & {
    'graphql-proxy'?: string;
};
export declare type Proxies = Record<string, {
    target: string;
    changeOrigin: boolean;
}>;
