const prettyCatch = async (cb) => {
    const response = [undefined, undefined];
    await cb()
        .then((data) => {
        response[1] = data;
    })
        .catch((e) => {
        response[0] = e;
    });
    return response;
};
export default prettyCatch;
