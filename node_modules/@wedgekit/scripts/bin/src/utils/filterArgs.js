const checkFlag = (args, filters, index) => {
    if (args[index][0] === '-') {
        return !filters.includes(args[index].replace(/^-*/g, ''));
    }
    return checkFlag(args, filters, index - 1);
};
export default (args, filters) => args.filter((arg, index) => checkFlag(args, filters, index));
