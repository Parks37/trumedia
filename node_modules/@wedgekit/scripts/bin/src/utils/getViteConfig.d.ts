import type { StartOptions, Proxies } from '../index';
declare const getViteConfig: (opts: StartOptions) => {
    build: {
        commonjsOptions: {
            transformMixedEsModules: boolean;
        };
    };
    define: void;
    server: {
        fs: {
            allow: string[];
        };
        proxy: Proxies;
    };
    optimizeDeps: {
        include: string[];
    };
    resolve: {
        dedupe: string[];
        extensions: string[];
    };
};
export default getViteConfig;
