import chalk from 'chalk';

import messages from './messages';

export default () => {
  console.log(messages.success(`${'-'.repeat(17)}\n* Hello, World! *\n${'-'.repeat(17)}`));
  console.log(
    messages.text(`This is a template script from ${chalk.bold('@wedgekit/scripts')} create\n`),
  );
};
