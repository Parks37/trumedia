import React from 'react';
import { Router } from 'react-router-dom';
import { History } from 'history';

function App({ history, sessionData }: { history: History<unknown>; sessionData }) {
  return (
    <Router history={history}>
      <div>Hello World</div>
    </Router>
  );
}

export default App;
