import type { Bearings, Position } from '../Popover';
export declare const DEFAULT_OFFSET = 16;
declare const findPosition: (anchor: DOMRect, floater: DOMRect, bearings?: Bearings | undefined) => Position;
export default findPosition;
