declare type OptionProps = {
    disabled?: boolean;
    label: string;
    id: string;
};
declare type NewValueType = {
    allowMultiple?: boolean;
    options: OptionProps[];
    filteredOptions?: OptionProps[];
    currentIdx: number;
    previousIdx: number;
    shiftDown: boolean;
    value: string[];
};
declare const getNewValue: ({ allowMultiple, options, filteredOptions, currentIdx, previousIdx, shiftDown, value, }: NewValueType) => string[];
export default getNewValue;
