/// <reference types="react" />
import { IconProps } from '../Icon';
interface Props extends Omit<IconProps, 'children'> {
    direction: 'up' | 'down';
}
declare const Sort: (props: Props) => JSX.Element;
export default Sort;
