import o from"ava";import i from"./mutatePositionForClipping";o("mutationPositionForClipping: should offset the style when `top` is clipping",t=>{t.deepEqual(i({top:-10,left:8},["top"],8),{top:8,left:8})}),o("mutationPositionForClipping: should offset the style when `bottom` is clipping",t=>{t.deepEqual(i({top:9e3,left:8},["bottom"],8),{bottom:8,left:8})}),o("mutationPositionForClipping: should offset the style when `right` is clipping",t=>{t.deepEqual(i({top:8,left:9e3},["right"],8),{top:8,right:8})}),o("mutationPositionForClipping: should offset the style when `left` is clipping",t=>{t.deepEqual(i({top:8,left:-10},["left"],8),{top:8,left:8})}),o("mutationPositionForClipping: should offset the style when `left` and `bottom` are clipping",t=>{t.deepEqual(i({top:9e3,left:-10},["left","bottom"],8),{bottom:8,left:8})}),o("mutationPositionForClipping: should offset the style when `top` and `right` are clipping",t=>{t.deepEqual(i({top:-12,left:9e3},["top","right"],8),{top:8,right:8})}),o("mutationPositionForClipping: should offset the style when `left` and `right` are clipping. In this case, the last clipping edge in competing edges will receive the offset",t=>{t.deepEqual(i({top:8,left:-12},["left","right"],8),{top:8,right:8})});
