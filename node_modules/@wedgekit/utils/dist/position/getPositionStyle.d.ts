import { Position, PositionOffset } from './types';
declare const _default: (position: string, toggleCoordinates: Position, offset: number, align: string, popover: PositionOffset, toggle: PositionOffset) => Position;
export default _default;
