import { Position, ViewportEdge } from './types';
declare const mutatePositionForClipping: (positionStyle: Position, clippedEdges: ViewportEdge[], offset: number) => Position;
export default mutatePositionForClipping;
