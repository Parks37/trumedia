import { KeyboardEvent, KeyboardEventHandler } from 'react';
export declare const processKeys: <E>(callbackRecord: Record<string, (event: KeyboardEvent<E>) => void>) => {
    key: RegExp;
    callback: (event: KeyboardEvent<E>) => void;
}[];
/**
 * Create returns a function that will execute the `callback` value(s) if the `event.key`
 * matches one or more of the keys of the argument. The keys of the argument can either be a
 * standard string or a regex parsable string.
 *
 * @see https://developer.mozilla.org/en-US/docs/Web/API/KeyboardEvent/key/Key_Values
 */
declare const handleKeyEvent: <E extends HTMLElement = HTMLElement>(callbackRecord: Record<string, (event: KeyboardEvent<E>) => void>) => KeyboardEventHandler<E>;
export default handleKeyEvent;
